# Catalog Hub — CompanyStore.IO

Client-facing flipbook catalog hub. Static frontend (GitHub Pages); backend is a Google Apps Script JSON API reading a Drive folder + Catalog Index sheet.

- `?c=<collection>` — curated view for one client
- `?cat=<CATALOG-ID>` — deep-link a catalog (add `&c=` for hidden ones)
